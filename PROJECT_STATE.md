# PROJECT STATE

## Proyecto
`job-analytics-platform`

## Estado actual
v1 funcional (checkpoint base `71728c5`). v1.1 y v1.2 implementadas y validadas.

## Iteración actual
v1.2 — Pytest Setup + First Tests (cerrada)

## Plan
`docs/v1_2_plan.md`

## Completado
- v1.1: logging estructurado en `run_pipeline.py` + health check de PostgreSQL en `main.py`.
- v1.2: setup de pytest (`pytest.ini`, `testpaths=tests`) + test trivial + primer test real (mock) sobre `transform_jobs`.

## Pendiente inmediato
Ninguno — definir alcance de v1.3.

## Próximo paso
Definir y documentar el plan de v1.3 en `docs/` antes de tocar código.

## Blockers
Ninguno.
